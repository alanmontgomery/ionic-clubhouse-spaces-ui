import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-clubhouse-spaces-ui',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
